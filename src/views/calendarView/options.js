const header = {
    left: "prev,next, today",
    center: "title",
    right: "dayGridMonth,timeGridWeek,timeGridDay"
  };
  const footer = {
    left: "Brian, Chrystal, Jeff, Shawn"
  };
  
  export{
      header,
      footer
  }